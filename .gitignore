# Never commit local Claude Code settings — they often contain secrets
# (API keys, SSH passwords, host IPs) in permissions/env.
settings.local.json
**/settings.local.json

# Runtime state produced by the hook
state/
**/pop_quiz_state.json
*.tmp

# Personal learning journal (your real answers) — keep private.
# The committed JOURNAL.example.md shows the format; your real log stays local.
JOURNAL.md
learning_journal.md

# Python
__pycache__/
*.pyc

# Editor / OS cruft
.DS_Store
.vscode/
.idea/
